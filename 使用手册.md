# 晶圆判定工作台操作手册

> 适用系统：霸州晶圆良品判定可视化系统
> 文档版本：2026-08-12
> 适用对象：测试工程师、品质工程师、设备工程师及系统管理员

## 1. 系统用途

本系统用于读取晶圆测试数据，按照参数模板中的上下限自动判定每颗 Die 的 Pass/Fail，并通过晶圆图谱展示结果。

主要功能包括：

- 按时间和 Wafer 筛选测试数据；
- 管理参数模板及 Min/Max 判定范围；
- 上传、选择、重命名和删除 Map 布局模板；
- 在晶圆图谱中查看 Pass、Fail、未测试和 Test Key；
- 将指定 Die 人工设为未测试，或恢复为正常已测试状态；
- 查看良率及各参数不良率；
- 保存判定快照并导出 Excel；
- 每 30 秒自动拉取最新数据。

## 2. 启动、访问与停止

### 2.1 一键启动

进入系统目录，双击：

```text
一键启动.bat
```

脚本会检查运行环境、启动后端和前端，并自动打开浏览器。首次启动可能需要安装依赖，请等待两个服务窗口启动完成，不要中途关闭。

### 2.2 访问地址

根据启动方式使用以下地址之一：

- 一键启动或前端开发模式：`http://localhost:5173/`
- 后端集成页面：`http://127.0.0.1:8000/`

页面右上角出现“后端已连接”表示系统可正常操作。

### 2.3 停止系统

双击：

```text
停止服务.bat
```

也可以关闭标题中带有 `WaferBackend8000` 和 `WaferFrontend5173` 的两个命令窗口。

## 3. 页面区域说明

页面按业务流程分为顶部操作区和左、中、右三个工作区：

```text
顶部：01 筛选 → 02 数据 → 03 执行

左侧：参数配置     中间：晶圆图谱     右侧：统计结果
```

### 3.1 顶部状态

- “连接配置”：位于后端连接状态左侧，用于配置 Mock 或 MySQL 数据源；
- “后端已连接”：显示当前后端接口地址；
- 绿色布局状态：显示当前 Map 文件、Shot 数、Die 行列和 Test Key 数；
- 如果数据库无法访问，系统可能自动切换为 Mock 样例数据，请确认当前数据源后再用于正式判定。

### 3.2 01 筛选

- 时间范围：按 `CreateTime` 限定数据范围；留空表示不限制时间；
- 选择 Wafer：选择需要查看的晶圆；
- 选择参数模板：选择本次判定使用的规格模板。

### 3.3 02 数据

- 拉取数据：立即从当前数据源读取最新数据并重新判定；
- 选择 Map 模板：切换已经保存的布局模板；
- 上传并保存 Map：上传 TXT/TSV 文件并自动保存成 Map 模板；
- 重命名：修改当前 Map 模板的显示名称；
- 删除模板：删除当前 Map 模板。

### 3.4 03 执行

- 确认并刷新结果：使用当前 Wafer、参数模板、参数开关、Min/Max 和 Map 模板重新判定。

执行前必须至少启用一项参数，否则系统提示“请至少启用一项判定参数”，且不会执行判定。

## 4. 标准日常操作流程

建议每次按以下顺序操作。

### 第一步：确认数据连接

1. 点击顶部“连接配置”；
2. 演示或培训时打开“Mock 模式”；
3. 使用现场数据时关闭“Mock 模式”，填写：
   - 主机；
   - 端口；
   - 数据库；
   - 用户名；
   - 密码；
4. 点击“测试连接”；
5. 测试成功后点击“保存并加载”。

现场数据库通常为 `mg_nano`，端口通常为 `3306`，实际账号由数据库管理员提供。

> 注意：Mock 数据仅用于演示和功能验证，不应用于正式生产结论。

### 第二步：选择时间和 Wafer

1. 如需限制数据时间，设置 `CreateTime 起` 和 `CreateTime 止`；
2. 在“选择 Wafer”中选择目标晶圆；
3. 如需立即读取最新数据，点击“拉取数据”。

切换 Wafer 或时间范围后，系统会重新判定当前数据。

### 第三步：选择参数模板

在“选择参数模板”中选择需要使用的客户标准。

默认模板会显示“[默认]”，默认模板：

- 可以修改页面中的参数用于临时判定；
- 不允许直接覆盖；
- 不允许删除；
- 如需长期保存修改内容，应使用左侧“另存”。

### 第四步：选择 Map 模板

在“选择 Map 模板”中选择与当前产品、晶圆和测试布局相匹配的模板。

选择后系统会加载：

- Shot 位置；
- 每个 Shot 内的 Die 行列；
- Die 流水号位置；
- Test Key 位置；
- 当前布局保存的晶圆外圈设置。

### 第五步：检查参数配置

检查左侧每项参数的：

- 启用状态；
- Min；
- Max；
- MPD Dark Current 的 1V/4V 两组限值。

不需要参与本次判定的参数可以关闭。不得把所有参数都关闭。

### 第六步：执行判定

点击“确认并刷新结果”。判定成功后：

- 中间晶圆图谱更新；
- 右侧 Total、Pass、Fail、Yield 更新；
- 不良率详情更新；
- 图谱下方“最近更新”显示本次更新时间，精确到年月日时分秒。

## 5. 判定逻辑与统计口径

### 5.1 严格开区间判定

系统使用严格开区间：

```text
Min ＜ 实测值 ＜ Max
```

没有等号，因此：

- 实测值等于 Min：Fail；
- 实测值等于 Max：Fail；
- 实测值严格位于 Min 和 Max 之间：Pass；
- Min 留空：只判断“实测值 ＜ Max”；
- Max 留空：只判断“Min ＜ 实测值”；
- 实测值缺失：Fail。

一颗 Die 的全部启用参数都为 Pass，该 Die 才是 Pass；任何一个启用参数 Fail 或无实测值，Die 总体即为 Fail。

### 5.2 统计指标

- `Total Dies`：本次实际参与统计的 Die 数；
- `Pass Dies`：参与统计且总体 Pass 的 Die 数；
- `Fail Dies`：参与统计且总体 Fail 的 Die 数；
- `Yield`：`Pass Dies ÷ Total Dies × 100%`；
- 参数不良率：该参数 Fail 的 Die 数 ÷ `Total Dies`。

以下位置不进入 Total、Pass、Fail 和不良率：

- Map 中没有测试数据的真正未测试位置；
- 人工设置为“未测试(人为)”的 Die；
- Test Key。

### 5.3 Shot 不在布局中的提示

如果页面顶部提示：

```text
有 N 个 Shot 号不在布局中（例如 31），这些点不会画在图谱上。
```

其中 `31` 等编号来自数据库测试数据中的 Shot 字段。当前 Map 模板没有该 Shot 的坐标，因此它不会显示在晶圆图谱上。

这类有实际测试记录但未匹配布局的 Die 仍可能进入右侧统计。应检查：

1. 是否选错 Map 模板；
2. 上传的布局是否缺少该 Shot；
3. 数据库 Shot 编号是否正确。

## 6. 参数模板管理

### 6.1 修改参数

左侧“参数配置”支持：

- 打开或关闭参数；
- 修改 Min/Max；
- 修改 MPD Dark Current 的 1V/4V Min/Max；
- 删除自己添加的自定义参数。

页面中修改参数后，如果只需要本次使用，直接点击“确认并刷新结果”即可；如果以后还要使用，需要保存为参数模板。

### 6.2 添加数据库 ItemName

1. 点击左侧“添加”；
2. 在弹窗中选择数据库已有的 `ItemName`；
3. 点击“添加”；
4. 设置该项 Min/Max；
5. 确认其“启用”开关已打开；
6. 点击“确认并刷新结果”。

自定义项会显示“自定义”标签，并在行尾提供“删”按钮。内置标准项不能删除，只能关闭。

### 6.3 另存参数模板

1. 调整好当前参数配置；
2. 点击左侧“另存”；
3. 输入模板名称；
4. 点击“保存”。

保存内容包括：参数列表、启用状态、Min/Max 和自定义 Item。新模板随后会出现在顶部“选择参数模板”中。

### 6.4 更新参数模板

1. 先选择一个非默认参数模板；
2. 修改参数；
3. 点击“更新”；
4. 确认模板名称并保存。

“更新”只覆盖当前选中的用户模板，不影响其他参数模板。

### 6.5 删除参数模板

1. 选择一个非默认参数模板；
2. 点击左侧“删除”。

删除完成后系统会切回默认参数模板。默认模板不能删除。

## 7. Map 模板管理

### 7.1 上传并保存 Map

1. 点击顶部“上传并保存 Map”；
2. 选择 UTF-8 编码的 `.txt` 或 `.tsv` 布局文件；
3. 等待“已保存 Map 模板”提示；
4. 上传成功后，该模板会立即出现在“选择 Map 模板”下拉框中，并自动成为当前模板。

Map 文件需符合本系统 Level1/Level2 布局格式，示例文件位于：

```text
examples\SF_DR8.txt
```

### 7.2 上传文件如何生成模板

- 不同文件名：分别保存为不同 Map 模板；
- 相同文件名再次上传：更新原 Map 模板，不增加重复模板；
- 默认显示名称：上传文件去掉扩展名后的名称；
- 上传成功后无需每次重新上传，下次直接在“选择 Map 模板”中选择。

### 7.3 重命名 Map 模板

1. 在“选择 Map 模板”中选中模板；
2. 点击“重命名”；
3. 输入新名称；
4. 点击“保存”。

重命名只改变下拉框中的显示名称，不改变布局内容。

### 7.4 删除 Map 模板

1. 选中需要删除的 Map 模板；
2. 点击“删除模板”；
3. 在确认框中确认删除。

删除不可恢复。如果删除的是当前模板：

- 还有其他模板时，系统自动切换到其他模板；
- 没有其他模板时，图谱清空，需重新上传 Map 后才能判定。

### 7.5 Map 模板保存位置

Map 模板由后端保存，不保存在浏览器临时缓存中：

```text
backend\data\layout_templates\*.json
```

当前选中的 Map 模板编号保存在：

```text
backend\data\current_layout_id.txt
```

系统重启后仍可继续选择已经保存的 Map 模板。

## 8. 晶圆图谱说明

### 8.1 颜色含义

| 颜色 | 状态 | 是否参与统计 | 是否可操作 |
| --- | --- | --- | --- |
| 绿色 | Pass | 是 | 可查看详情、可设为未测试 |
| 红色 | Fail | 是 | 可查看详情、可设为未测试 |
| 浅灰色 | 未测试 | 否 | 无原始测试数据，不能查看详情 |
| 深灰色 | 未测试(人为) | 否 | 可恢复为已测试 |
| 黄色 | Test Key | 否 | 不可点击 |

蓝色边框用于区分 Shot，红色圆圈表示晶圆外圈。

### 8.2 查看 Die 详情

1. 点击晶圆图中的目标 Die；
2. 系统打开该 Shot 的“选择 Die”窗口；
3. 点击 Die 的名称/状态区域；
4. 查看芯片详情中的 Item、Condition、Unit、Min、Target、Max、Note 和结果。

详情中的“总体评价”遵循“全部启用 Item 都 Pass，Die 才 Pass”的规则。

### 8.3 人工设为未测试

此操作只能在“选择 Die”窗口完成，不在芯片详情窗口中操作。

1. 点击晶圆图中的目标 Die，打开“选择 Die”窗口；
2. 找到目标 Die；
3. 点击“设为未测试”；
4. 系统弹出：

   ```text
   确定设为未测试吗？
   确定后，计算不良率时将不统计该 Die。
   ```

5. 点击“确定”。

完成后：

- Die 显示为“未测试(人为)”；
- 从 Total、Pass、Fail、Yield 和参数不良率中排除；
- 状态保存到后端；
- 点击“拉取数据”、点击“确认并刷新结果”或等待 30 秒自动更新都不会清除该状态。

### 8.4 恢复已测试

1. 点击图中的“未测试(人为)”Die；
2. 在“选择 Die”窗口点击“恢复已测试”；
3. 系统重新判定该 Die，并将其恢复到正常统计。

恢复后的 Pass/Fail 由原始测试数据和当前参数模板重新计算，不由人工指定。

### 8.5 图形设置

点击“图形设置”可以调整：

- 外圈缩放；
- 水平偏移；
- 垂直偏移。

操作说明：

- 拖动弹窗标题栏可以移动设置窗口；
- 调节滑块时晶圆图即时预览，无需先关闭弹窗；
- 设置只影响红色晶圆外圈，不改变 Shot/Die 的布局坐标；
- “恢复自动值”将缩放和偏移恢复为默认值；
- 点击“保存”后，设置按当前 Map 模板保存，同一套后端服务的所有电脑共用。

## 9. 自动更新与最近更新时间

页面打开后，每 30 秒自动执行一次最新数据拉取和判定。

以下操作会刷新“最近更新”时间：

- 30 秒自动拉取；
- 人工点击“拉取数据”；
- 点击“确认并刷新结果”；
- 其他触发重新判定的操作。

时间显示格式：

```text
YYYY-MM-DD HH:mm:ss
```

自动拉取只更新数据和判定，不会重置：

- 当前参数配置；
- 当前参数模板；
- 当前 Map 模板；
- 已保存的“未测试(人为)”Die 状态。

## 10. 统计结果、保存与导出

### 10.1 查看统计结果

右侧显示：

- Total Dies；
- Pass Dies；
- Fail Dies；
- Yield；
- 各参数的不良数量及不良率。

### 10.2 “保存”的含义

右侧“保存”用于将当前判定结果快照保存到本机后端数据库，便于审计和回溯。它不会创建参数模板，也不会创建 Map 模板。

### 10.3 导出 Excel

点击“导出”后下载：

```text
<Wafer>_judge.xlsx
```

Excel 包含：

- 判定汇总；
- 全部 Die；
- 不良品清单。

人工未测试的 Die 在“全部 Die”中显示为“未测试”，不会进入“不良品清单”。

### 10.4 四种“保存”不要混淆

| 操作位置 | 按钮/操作 | 保存内容 |
| --- | --- | --- |
| 左侧参数配置 | 另存/更新 | 参数模板 |
| 顶部数据区 | 上传并保存 Map | Map 布局模板 |
| 图形设置弹窗 | 保存 | 当前 Map 的晶圆外圈设置 |
| 右侧统计结果 | 保存 | 当前判定结果快照 |

## 11. 常见问题与处理

### 11.1 页面打不开

1. 确认已经运行“一键启动.bat”；
2. 确认后端和前端两个命令窗口没有被关闭；
3. 尝试访问 `http://localhost:5173/`；
4. 集成运行时尝试访问 `http://127.0.0.1:8000/`；
5. 运行“检测连接.bat”查看服务状态。

### 11.2 Wafer 下拉框为空

- 点击“连接配置”测试连接；
- Mock 模式下确认样例文件存在；
- 真库模式下确认账号可读取 `summaryhead` 和 `summarydetail`；
- 检查时间范围是否排除了所有数据。

### 11.3 改了 Min/Max，但图谱没变化

点击“确认并刷新结果”。如果仍未变化，确认修改的是当前启用参数，并检查实测值是否真的跨过严格边界。

### 11.4 为什么等于 Min 或 Max 仍然 Fail

系统规则是 `Min＜值＜Max`，等于边界不合格。

### 11.5 为什么参数全部关闭后全是 Fail，或不能刷新

当前版本要求至少启用一项参数。全部关闭时系统会阻止判定并提示“请至少启用一项判定参数”。重新打开至少一项后再刷新。

### 11.6 为什么几乎全部是 Fail

依次检查：

1. 是否选错参数模板；
2. Min/Max 是否填反；
3. 实测值是否等于边界；
4. 数据单位与规格单位是否一致；
5. 启用参数是否缺少实测数据；
6. 点击某颗红色 Die 查看 Note 中的实测值。

### 11.7 上传 Map 后为什么模板数量没有增加

- 同名文件重复上传会更新原模板，不会增加数量；
- 需要新建模板时，请使用不同文件名；
- 上传成功后查看“选择 Map 模板”下拉框；
- 如果上传失败，检查文件扩展名、UTF-8 编码及 Level1/Level2 格式。

### 11.8 Map 模板选错了怎么办

直接在“选择 Map 模板”中选择正确模板。无需重新上传。

### 11.9 点击 Die 无法查看详情

- 黄色为 Test Key，不能查看；
- 浅灰色“未测试”没有原始数据，不能查看；
- 如果数据库 Shot 不在当前布局中，该 Die 不会画在图上；
- 选择正确 Map 模板后重新刷新。

### 11.10 人工未测试状态刷新后会不会丢失

不会。状态按 Wafer 和 Die 保存到后端，人工拉取、确认刷新、30 秒自动拉取及页面刷新后仍会保留，直到点击“恢复已测试”。

### 11.11 真库连接失败后为什么出现 Mock 数据

系统启动时如果数据库不可用，可能自动切换到 Mock 样例数据。请检查右上角状态和“连接配置”，确认重新连接真库后再进行正式分析。

## 12. 数据保存与备份

管理员建议定期备份：

```text
backend\data\
```

主要内容：

- `local.db`：数据库配置、参数模板、判定快照、图形设置、人工未测试状态和审计日志；
- `layout_templates\`：Map 模板；
- `current_layout_id.txt`：当前选中的 Map 模板；
- `current_layout.json`：当前布局的兼容副本。

备份前建议停止系统，避免复制过程中数据仍在写入。

## 13. 生产使用检查清单

正式查看或导出前确认：

- [ ] 页面右上角显示后端已连接；
- [ ] 当前不是误用 Mock 数据；
- [ ] 时间范围正确；
- [ ] Wafer 正确；
- [ ] 参数模板正确；
- [ ] Map 模板正确；
- [ ] 至少一项参数启用；
- [ ] Min/Max 符合客户规格，理解等于边界会 Fail；
- [ ] 页面无 Shot 不在布局的异常提示，或该提示已核实；
- [ ] 人工未测试 Die 已复核；
- [ ] “最近更新”时间为本次操作时间；
- [ ] 再点击一次“确认并刷新结果”后保存或导出。

## 14. 报障时需要提供的信息

联系系统维护人员时请提供：

- Wafer 编号；
- 当前时间范围；
- 当前参数模板名称；
- 当前 Map 模板名称；
- 当前是 Mock 还是真库；
- 页面完整截图；
- 弹窗或顶部提示原文；
- “最近更新”时间；
- 是否能打开 `http://127.0.0.1:8000/api/health`。

技术启动、数据库结构及开发说明参见同目录 `README.md`。
