# 晶圆良品判定可视化工具 — 使用手册

**连库 → 设规格 → 看晶圆图 → 点晶粒看详情 → 导出结果**。

---

## 1. 这个工具做什么？

晶圆测试机台的数据存在 MySQL 数据库里。以前要人工对照规格表逐颗判断良/不良，既慢又容易漏。

本工具可以：

1. 读入某片晶圆（Wafer）上每颗芯片（Shot）的测试参数  
2. 按你设定的 **下限 Min / 上限 Max** 自动判定 Pass / Fail  
3. 在 **晶圆图** 上用颜色标出良品（绿）和不良品（红）  
4. 点击某一颗，查看该颗的完整参数表与 **总体评价**  
5. 把结果导出成 Excel，方便存档或失效分析  

**判定规则（请记住）：**  
当前启用的每一项参数都必须在 Min～Max 范围内，该颗芯片才算 **Pass**；只要有一项超限或缺测，总体就是 **Fail**。

---

## 2. 使用前准备

### 2.1 找 IT / 同事帮你做的事（第一次）

双击项目里的 **`一键启动.bat`**（或上一级文件夹同名文件）即可：自动装好环境并启动前后端，浏览器一般会自动打开。

也可让同事按 `wafer-yield/README.md` 手动启动。成功后地址为：

**http://localhost:5173/**

停止时关闭弹出的两个黑色窗口，或双击 `停止服务.bat`。

**拷贝到工程师电脑时注意：** 不要连同 `backend\.venv`、`frontend\node_modules` 一起拷（这两份是本机环境，换电脑会坏）。把整包拷过去后直接再跑一次 `一键启动.bat` 即可在本机重新生成。若仍报错，可先删掉工程师电脑上的 `wafer-yield\backend\.venv` 再双击一键启动。

建议用 Chrome 或 Edge。

### 2.2 你需要知道的信息（接真机数据库时）

向设备 / 数据管理员索取：

| 项目 | 说明 | 示例 |
| --- | --- | --- |
| 主机地址 | 数据库所在电脑的 IP | `192.168.x.x` |
| 端口 | 一般是 3306 | `3306` |
| 数据库名 | 固定为 mg_nano 时填这个 | `mg_nano` |
| 用户名 / 密码 | 只读账号即可 | （内部提供） |

> 若暂时连不上真库，可用 **Mock 模式** 先熟悉界面（用的是内置样例数据，不是现场真数据）。

---

## 3. 界面一览（三块区域）

打开网页后，大致分为：

```
┌─────────────────────────────────────────────────────────┐
│ 顶部：选晶圆、选规格模板、数据连接、刷新判定              │
├──────────────┬────────────────────┬─────────────────────┤
│ 左侧           │ 中间                │ 右侧                │
│ 参数配置       │ 晶圆图谱            │ 统计结果            │
│ Min / Max      │ 绿=Pass 红=Fail     │ 良率、不良率明细    │
└──────────────┴────────────────────┴─────────────────────┘
```

---

## 4. 日常操作步骤（推荐顺序）

### 步骤 A：连接数据

1. 点击右上角 **「数据连接」**  
2. 若要用样例练习：打开 **Mock 模式** → **测试连接** → **保存并加载**  
3. 若要接真机库：关闭 Mock → 填写主机 / 端口 / 库名 / 用户名 / 密码 → **测试连接**（成功后再 **保存并加载**）  

密码会加密保存在本机配置里，不会写进程序代码。

### 步骤 B：选择晶圆

顶部下拉框选择 **Wafer**（例如 `DR4-5-26`）。  
换一片晶圆后，点一次 **「确认配置 / 刷新」**。

### 步骤 C：检查 / 修改规格限

左侧 **参数配置** 默认带有 DR8-PIC 标准项（含 On Chip Loss、MZM Loss、Out/In MPD Imbalance、MPD Dark Current 两档、消光比、Heater 等）。

对每一行你可以：

- **启用**：关掉则本项不参与判定  
- **Min**：下限（没有就留空）  
- **Max**：上限（没有就留空）  

改完后务必点 **「确认配置 / 刷新」**，晶圆图和统计才会更新。

#### 从数据库追加参数（可选）

若标准项不够，还想用库里已有的其它 `ItemName`（例如 `IL`、`LoopBack3`）：

1. 点 **「从数据库添加」**  
2. 勾选需要的参数名 → **添加**  
3. 给新行填好 Min / Max，打开「启用」  
4. 再点 **「确认配置 / 刷新」**  

自定义项带「自定义」标记，可删除；标准项只能关启用，不能删。

觉得这组限值常用：

1. 点 **「另存为新模板」**，起一个名字（如「客户A标准」）并保存  
2. **默认「DR8-PIC 客户标准」始终保留、不会被改动**；改某个客户模板也只影响该模板  
3. 下次打开或刷新页面，会自动回到你上次选用的模板（不必再切回默认）  

若改的是自己以前保存的模板，可用 **「更新当前模板」** 覆盖；默认模板只能另存，不能更新或删除。

### 步骤 D：看晶圆图

中间圆形图：

- **绿色方块** = 该颗总体 Pass  
- **红色方块** = 该颗总体 Fail  
- 鼠标移上去会高亮当前晶粒边框  

右侧可看：

- 总颗数、Pass 数、Fail 数、**良率 %**  
- 各参数导致不良的比例（不良率详情）  

### 步骤 E：点开某一颗看详情

点击晶圆图上的某一颗，弹出详情表，表头为：

| Item | Condition | Unit | Min | Target | Max | Note |
| --- | --- | --- | --- | --- | --- | --- |

说明：

- **Item**：参数名称  
- **Condition**：测试条件（如电压、光功率、波长；字母下标表示规格书里的下角标）  
- **Min / Target / Max**：规格下限 / 目标 / 上限（没有显示为 —）  
- **Note**：实测值与该项 Pass/Fail  
- 最后一行 **总体评价**：全部启用项都 Pass 才是 Pass  

### 步骤 F：保存与导出

- **保存数据**：把本次判定结果记到本机（方便回溯）  
- **数据导出**：下载 Excel，一般包含汇总、全部晶粒、不良品清单  

把文件存到你的实验记录目录即可。

---

## 5. 规格含义速查（默认 DR8 项）

以下为出厂默认理解，实际以客户规格书为准；可在左侧改 Min/Max。

| Item | 常见条件 | 默认限值理解 |
| --- | --- | --- |
| On Chip Loss + EC_EC Loss | V<sub>R,MOD</sub>=0、P<sub>in</sub>=6 dBm、1311 nm | 上限 14.4 dB |
| MZM Loss | 同上 | 上限 4.9 dB |
| OutMPD Imbalance | 同上 | -1 ～ 1 dB |
| InMPD Imbalance | 同上 | -1 ～ 1 dB |
| MPD Dark Current | V<sub>R,MPD</sub>=1.0 V | 上限 50 nA |
| MPD Dark Current | V<sub>R,MPD</sub>=4.0 V | 上限 200 nA |
| MOD ON_OFF Extinction Ratio | 同第一行条件 | 下限 24 dB，目标 30 |
| Heater Pi Shift Power | （无特殊 Condition） | 目标 10 mW，上限 14.5 |

> 库里的原始字段名可能写成 `OnChipLoss`、`EC-EC` 等；工具会自动换算「On Chip Loss + EC_EC Loss」等标准项。若某项 Note 里一直缺测，请核对数据库是否真有对应 `ItemName`。

---

## 6. 常见问题

**Q：页面打不开？**  
A：服务可能没启动。找同事重新启动前后端，或确认地址是否为 `http://localhost:5173/`。

**Q：晶圆下拉是空的？**  
A：先到「数据连接」测试连接是否成功；Mock 模式下应能看到样例 Wafer。真库模式请确认账号有读 `summaryhead` / `summarydetail` 的权限。

**Q：改了 Min/Max，图颜色没变？**  
A：必须再点一次 **「确认配置 / 刷新」**。

**Q：为什么几乎全红？**  
A：常见原因：限值设反了（Min/Max）、单位不一致、或库内数值符号与规格书定义不同（例如损耗有正有负）。先点一颗绿/红晶粒看 Note 里的实测值，再对照规格书调整。

**Q：某一项一直 Fail，但我觉得数是对的？**  
A：看详情里该项的 Min/Max 与实测。缺测也会判 Fail。自定义参数要确认添加的 `ItemName` 与库里完全一致（大小写、连字符）。

**Q：Mock 和真库有什么区别？**  
A：Mock 只用于界面演示；写报告、放行必须以真库数据为准。

**Q：能不能多人同时用？**  
A：可以打开同一个网页地址使用；改规格、导出互不影响各自浏览器操作。共享模板需点「保存模板」并由同一套服务读取。

---

## 7. 建议工作流（产线日常）

1. 打开工具 → 确认已连真库（Mock 关闭）  
2. 选当天要看的 Wafer  
3. 选用「客户 A / DR8-PIC」等模板，或微调 Min/Max  
4. 刷新 → 看良率是否异常  
5. 点红色晶粒，记录超限参数  
6. 导出 Excel 存档  

---

## 8. 需要帮助时请提供这些信息

联系 IT / 开发同事时，尽量说明：

- 晶圆编号（Wafer）  
- 问题截图（晶圆图或详情表）  
- 是 Mock 还是真库  
- 「测试连接」的提示原文  

---

**文档版本：** 与当前 `wafer-yield` 工具配套  
**相关说明：** 更技术的启动方式见同目录 `README.md`；系统设计见上级目录规划文档。
